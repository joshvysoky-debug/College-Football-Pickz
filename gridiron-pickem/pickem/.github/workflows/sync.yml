name: Sync CFB scores
on:
  schedule:
    - cron: '0 */3 * * *'
  workflow_dispatch: {}
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Call sync endpoint
        run: curl -sf "https://YOUR-APP-URL.vercel.app/api/sync?secret=${{ secrets.CRON_SECRET }}"
